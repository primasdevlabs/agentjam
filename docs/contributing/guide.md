# Contributor Guide

Thank you for contributing to AgentJam!

## Adding a New Resource

1. Copy a template from `templates/` into the target directory (`agents/`, `skills/`, `tools/`, or `workflows/`).
2. Populate `*.yaml` manifest metadata and markdown instructions.
3. Run `npm run validate` to ensure structural integrity.
4. Submit a Pull Request.
