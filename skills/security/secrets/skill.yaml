name: secrets
version: 1.0.0
type: skill
description: Prevents accidental commitment of API keys, private credentials, tokens, or environment secrets.
category: security

triggers:
  - scan-secrets
  - check-credentials
