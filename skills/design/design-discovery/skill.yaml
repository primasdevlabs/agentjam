name: design-discovery
version: 1.0.0
type: skill
description: Interactive and automated design system discovery for brand, typography, color, spacing, icon, and UI component conventions.
category: design

triggers:
  - design-discovery
  - ui-direction

parameters:
  inspectCodebaseFirst:
    type: boolean
    description: Inspect existing tokens and component files before asking the user.
    default: true
