# Accessibility Instructions

## Guidelines
1. Minimum contrast ratio of 4.5:1 for normal text and 3:1 for large text.
2. Every interactive element must be keyboard navigable with explicit focus rings.
3. Use semantic HTML tags (`<nav>`, `<main>`, `<article>`, `<button>`) instead of nested `<div>` click handlers.
