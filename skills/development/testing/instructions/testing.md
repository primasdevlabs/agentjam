# Testing Instructions

## Protocol
1. Colocate test files alongside implementation files (`foo.test.ts` next to `foo.ts`).
2. Write unit tests for core domain logic and integration tests for external boundary adapters.
3. Verify test runner success (`vitest`, `jest`, `pest`, `pytest`, `cargo test`) before reporting completion.
