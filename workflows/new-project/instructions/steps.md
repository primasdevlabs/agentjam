# New Project Initialization Steps

1. Select framework and stack profile from `stacks/`.
2. Define `.agentjam/` local configuration directory and project governance rules.
3. Configure linting, formatting, typechecking, and test runner scripts upfront.
