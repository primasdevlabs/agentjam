# Step 1: Inspect Project Manifests & Stack

1. Read root manifests (`package.json`, `composer.json`, `pyproject.toml`, `go.mod`).
2. Identify framework versions, runtime version, and package manager.
3. Detect existing project configuration (`.eslintrc`, `tsconfig.json`, `.prettierrc`, `pint.json`).
