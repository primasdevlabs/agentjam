# Existing Project Audit Workflow

A mandatory AgentJam workflow executed when joining an existing codebase. Performs a non-destructive audit across tech stack, architecture, design tokens, dependencies, code quality, and conventions before implementing feature changes.
