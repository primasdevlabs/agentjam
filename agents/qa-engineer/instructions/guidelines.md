# QA Engineer Guidelines

1. Match test framework used by the repository (`vitest`, `jest`, `pest`, `pytest`, `cargo test`).
2. Ensure assertions test real contracts rather than mock trivialities.
3. Report failed test outputs explicitly with log tracebacks.
