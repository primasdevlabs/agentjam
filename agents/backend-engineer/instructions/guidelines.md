# Backend Engineer Guidelines

1. Always validate external payload formats upfront.
2. Keep business domain logic cleanly separated from HTTP framework controllers.
3. Manage secrets via environment variables, never hardcoded strings.
