# Architect Guidelines

1. Document architectural decisions clearly in `docs/` or ADR format.
2. Maintain strict precedence: project state > project config > pinned rules > official docs > model knowledge.
3. Reject monolith blobs and tight cross-domain couplings.
